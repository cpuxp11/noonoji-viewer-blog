---
const { title = '눈오지의 서재' } = Astro.props;
---
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <style>
      :root{
        --fg:#0f172a;
        --muted:#64748b;
        --border:#e2e8f0;
        --card:#ffffff;
        --brand:#1d4ed8;
      }
      *{box-sizing:border-box}
      body{
        font-family:-apple-system,BlinkMacSystemFont,'Apple SD Gothic Neo','Noto Sans KR',sans-serif;
        line-height:1.78;
        color:var(--fg);
        margin:0;
        background:#ffffff;
      }
      .container{max-width:960px;margin:16px auto;padding:0 16px 34px}
      a{color:var(--brand);text-decoration:none}
      a:hover{text-decoration:underline}
      .muted{color:var(--muted);font-size:14px}
      header{
        border:1px solid var(--border);
        border-left:6px solid #2563eb;
        border-radius:14px;
        padding:14px 16px;
        background:#f8fbff;
        margin-bottom:14px;
      }
      .top{display:flex;gap:8px;flex-wrap:wrap;margin:10px 0 2px}
      .pill{
        border:1px solid #bfdbfe;
        border-radius:999px;
        padding:6px 12px;
        background:#eff6ff;
        color:#1e3a8a;
        font-size:13px;
        font-weight:600;
      }
      .pill:hover{background:#dbeafe;text-decoration:none}
      .card{
        border:1px solid var(--border);
        border-radius:12px;
        padding:16px;
        background:var(--card);
        margin:10px 0;
      }
      h1,h2,h3{line-height:1.3}
      .site-title{margin:0 0 4px;letter-spacing:-0.01em}
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <a href="/"><h1 class="site-title">🐈 눈오지의 서재</h1></a>
        <div class="muted">운영 로그 · 수업 · 영상 아카이브</div>
        <nav class="top">
          <a class="pill" href="/all">전체</a>
          <a class="pill" href="/lessons">수업</a>
          <a class="pill" href="/worklog">업무일지</a>
          <a class="pill" href="/videos">영상</a>
        </nav>
      </header>
      <slot />
    </div>
  </body>
</html>
